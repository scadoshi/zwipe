target/
.env
.env.prod
.vscode
.DS_Store
cache/
*.ipa
*.aab
# Crate-local lockfiles are dead weight in a workspace (root Cargo.lock is the
# only one builds use) and generate ghost Dependabot alerts against stale graphs.
zerver/Cargo.lock
zwiper/Cargo.lock
zite/assets/guides/_staging/
