use dioxus::prelude::*;
use zwipe::domain::logo;

#[component]
pub fn AuthGate() -> Element {
    // build loading screen for while
    // authorization is being attempted
    // get jwt and do something with it if still valid
    todo!()
}
