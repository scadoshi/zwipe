# Copy this file to `.env` (in the same dir) and fill in real values.
# The `.env` file is gitignored — your real credentials never leave the machine.

ZWIPE_TEST_USER=test
ZWIPE_TEST_PASS=<your-test-password>
