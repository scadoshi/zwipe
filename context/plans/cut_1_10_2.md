# Checklist: cut + ship 1.10.2

**Status: SCOPING 2026-09-21. Nothing user-facing is built yet; the internal
work below is done and committed. Cut when the client-visible items land.**

Numbers: **1.10.2**, iOS **build 80**, Android **versionCode 43** (1.10.1
shipped as build 79 / vc42; confirm against
[`../operations/ios/app-store/submission/history.md`](../operations/ios/app-store/submission/history.md)).

## Why there is a 1.10.2 at all

The 2026-09-21 architecture work touches `zwiper`, so a client build is spent
whether or not anything user-facing rides along. That makes it worth pairing
with the small client fixes that are already specced rather than shipping a
release nobody can see.

## Already done (internal, no changelog line)

Nothing here is user-visible; it belongs in `overview.md`, not the changelog.

- The `zerver` feature flag is gone: 598 gates to zero, and `zwiper` no longer
  depends on the server crate at all.
- Passwords are unprintable. `Password` and the new `Secret` redact `Debug`
  and `Display`; five auth request types and eight contract fields adopted
  `Secret`.
- API paths are constants, and the integration suite calls them, so a wrong
  path fails a test instead of shipping a 404.
- Every call is an `Endpoint`; client transport collapsed to one function.

## Candidates to ride along

Each has a plan. None is built.

| Item | Plan | User-visible |
|---|---|---|
| iOS shake-to-undo prompt | [`ios_shake_to_undo.md`](ios_shake_to_undo.md) | yes |
| Catalog cache retry | [`catalog_cache_retry.md`](catalog_cache_retry.md) | yes |
| Featured-strip skeleton jump | todo.md (scoped 2026-09-01) | yes |
| "invalid oracle id" on add | [`invalid_oracle_id.md`](invalid_oracle_id.md) | yes, once the fix is chosen |

The oracle-id fix is the one to watch: its investigation step is a **server**
change (log the printing id on the 422), which can deploy before the cut and
may change which client fix is right. Do that first and let it inform the
scope.

## Changelog

Three entries are staged in `UPCOMING` in
`zwipe-core/src/content/changelog/mod.rs`. `UPCOMING` renders publicly with an
"Upcoming" badge, so an entry there is a promise: pull any item that gets cut.
The oracle-id line is deliberately absent until its fix is chosen.

At cut time, move the block from `UPCOMING` into `RELEASES` with the real
date, as 1.10.1 was.

## Cut

1. `git pull`, then the full gate: nightly fmt, clippy `-D warnings` on the
   workspace, workspace tests, and the client crates warning-free.
2. Bump `version` in the workspace `Cargo.toml` to 1.10.2.
3. Move the changelog block from `UPCOMING` to `RELEASES` with the ship date.
4. iOS: [`../operations/ios/app-store/submission/`](../operations/ios/app-store/submission/) build then publish.
   Standing gotchas: latest Xcode, wipe the cargo iOS cache after any Xcode
   update, Info.plist version patch, Transporter (never altool).
5. Android: [`../operations/android/play-store/submission/`](../operations/android/play-store/submission/).
   `zcripts/android/patch_bundle.sh` is not optional; skipping it silently
   reships the manifest bugs. Run the four verification greps.
6. What's New copy lifts from the changelog entries. Play copy avoids
   "Commander"; use "leader".

## After both stores approve

- Record LIVE in [`../progress/overview.md`](../progress/overview.md) and move the shipped entries there.
- Re-read the error and crash tables (due by 2026-10-06 regardless): whether
  the iOS no-unwind crash recurs now that 1.10.1+ carries the real cause, and
  whether "invalid oracle id" stops accruing.
- Store-build eyeball, still open from 1.10.1, plus the Pixel reinstall.
